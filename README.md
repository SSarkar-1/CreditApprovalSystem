# CreditApprovalSystem


This is a Django project that provides an API for credit approval and loan management. The API allows users to register customers, check their eligibility for loans, create loans, and view loan details.

## Installation

1. Clone the repository
2. Install the dependencies by running `pip install -r requirements.txt`
3. Start the server by running `python manage.py runserver`
